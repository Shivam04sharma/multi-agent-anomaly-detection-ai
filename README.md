# multi-agent-anomaly-detection-ai
Multi-agent AI pipeline for anomaly detection — 6 autonomous agents auto-select ML models (Isolation Forest, LOF, Z-score), fuse signals, and generate GPT-4o explanations on any tabular dataset.
